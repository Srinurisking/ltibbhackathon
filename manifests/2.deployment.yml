apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: default
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: mysqldb
        image: srinurisking/mine:app
        ports:
        - containerPort: 80
        env:
            - name: DB_HOST
              value: mysqldb
      imagePullSecrets:
            - name: dockerhub-secret
